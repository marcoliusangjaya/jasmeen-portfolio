import project from "./project";
import about from "./about";
import resume from "./resume";
import siteSettings from "./siteSettings";

export const schemaTypes = [project, about, resume, siteSettings];
